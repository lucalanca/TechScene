module.exports = {
    // Before generating any new files, remove files from previous build.
    example: ['<%= site.dest %>/*.html', '<%= site.dest %>/assets/css', '<%= site.dest %>/assets/images'],
};